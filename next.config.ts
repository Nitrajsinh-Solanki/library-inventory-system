import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: [
      'xcgtkffloifpunwssmpc.supabase.co',
      'covers.openlibrary.org'  
    ],
  },
};

export default nextConfig;

